{
 "id": "3402",
 "kind": "mcp",
 "name": "ddg-search",
 "fingerprint_md5": "adefdfa87aa8a484eafc041bacdff829",
 "download_count": 0,
 "category": "网页搜索",
 "tags": "",
 "version": "",
 "readme": "# DuckDuckGo搜索MCP服务器\r\n\r\n\r\n一种模型上下文协议（MCP）服务器，通过DuckDuckGo提供网络搜索功能，并具有用于内容获取和解析的附加功能。\r\n\r\n## 快速开始\r\n\r\n```bash\r\nuvx duckduckgo-mcp-server\r\n```\r\n\r\n## 特性\r\n\r\n- **网络搜索**：使用高级速率限制和结果格式搜索DuckDuckGo\r\n- **内容获取**：通过智能文本提取检索和解析网页内容\r\n- **速率限制**：内置对搜索和内容获取速率限制的保护\r\n- **错误处理**：全面的错误处理和记录\r\n- **LLM友好输出**：专门为大型语言模型使用而格式化的结果\r\n\r\n## 安装\r\n\r\n使用以下命令从PyPI安装 `uv`:\r\n\r\n```bash\r\nuv pip install duckduckgo-mcp-server\r\n```\r\n\r\n## 用法\r\n\r\n### 使用Claude Desktop运行\r\n\r\n1. 下载 [克劳德桌面版](https://claude.ai/download)\r\n1. 创建或编辑您的Claude Desktop配置：\r\n   - 在macOS上： `~/Library/Application Support/Claude/claude_desktop_config.json`\r\n   - 在Windows上： `%APPDATA%\\Claude\\claude_desktop_config.json`\r\n\r\n添加以下配置：\r\n\r\n**基本配置（无安全搜索，无默认区域）：**\r\n\r\n```json\r\n{\r\n    \"mcpServers\": {\r\n        \"ddg-search\": {\r\n            \"command\": \"uvx\",\r\n            \"args\": [\"duckduckgo-mcp-server\"]\r\n        }\r\n    }\r\n}\r\n```\r\n\r\n**使用安全搜索和区域配置：**\r\n\r\n```json\r\n{\r\n    \"mcpServers\": {\r\n        \"ddg-search\": {\r\n            \"command\": \"uvx\",\r\n            \"args\": [\"duckduckgo-mcp-server\"],\r\n            \"env\": {\r\n                \"DDG_SAFE_SEARCH\": \"STRICT\",\r\n                \"DDG_REGION\": \"cn-zh\"\r\n            }\r\n        }\r\n    }\r\n}\r\n```\r\n\r\n**配置选项：**\r\n\r\n- `DDG_SAFE_SEARCH`：安全搜索过滤级别（可选）\r\n  - `STRICT`：最大内容过滤（kp=1）\r\n  - `MODERATE`：平衡过滤（kp=-1，如果未指定，则默认值）\r\n  - `OFF`：无内容过滤（kp=-2）\r\n- `DDG_REGION`：默认地区/语言代码（可选，示例如下）\r\n  - `us-en`：美国（英语）\r\n  - `cn-zh`：中国（中文）\r\n  - `jp-ja`：日本（日语）\r\n  - `wt-wt`：无特定区域\r\n  - 为DuckDuckGo的默认行为留空\r\n\r\n3. 重新启动克劳德桌面\r\n\r\n### 使用Claude代码运行\r\n\r\n1. 下载 [克劳德代码](https://github.com/anthropics/claude-code)\r\n1. 确保 [`uvenv`](https://github.com/robinvandernoord/uvenv) 已安装 `uvx` 命令可用\r\n1. 添加MCP服务器： `claude mcp add ddg-search uvx duckduckgo-mcp-server`\r\n\r\n### 使用SSE或流式HTTP运行\r\n\r\n服务器支持与其他MCP客户端一起使用的替代传输：\r\n\r\n```bash\r\n# SSE transport\r\nuvx duckduckgo-mcp-server --transport sse\r\n\r\n# Streamable HTTP transport\r\nuvx duckduckgo-mcp-server --transport streamable-http\r\n```\r\n\r\n默认传输方式为 `stdio`，由Claude Desktop和Claude Code使用。\r\n\r\n跑步时 `sse` 或 `streamable-http`，覆盖默认绑定地址(`127.0.0.1:8000`)与 `--host` 和 `--port` 旗帜：\r\n\r\n```bash\r\nuvx duckduckgo-mcp-server --transport streamable-http --host 0.0.0.0 --port 7070\r\n```\r\n\r\n### 获取后端（绕过机器人检测）\r\n\r\n某些网站会阻止默认设置 `httpx` 客户端，因为其独特的TLS指纹，无论用户代理——Cloudflare Bot Management和类似的过滤器密钥是在JA3/TLS握手上，而不是在标头上。选择加入后端， `curl` （通过实施 `curl_cffi`)，模拟真实Chrome浏览器的TLS握手并通过这些检查。\r\n\r\n**安装：**\r\n\r\n```bash\r\n# Default install (httpx only)\r\nuv pip install duckduckgo-mcp-server\r\n\r\n# With the optional browser backend\r\nuv pip install \"duckduckgo-mcp-server[browser]\"\r\n```\r\n\r\n**后端选项：**\r\n\r\n|价值观|行为|需求 `[browser]` |\r\n| ------ | ----------------------------------------------------------------------------------------- | ----------------- |\r\n| `httpx` |轻量级异步HTTP。违约。适用于大多数网站。|没有|\r\n| `curl` |用途 `curl_cffi` Chrome 131 TLS模拟。通过基于TLS指纹的过滤器。|是的|\r\n| `auto` |尝试 `httpx` 第一；在403或Cloudflare质询响应上，重试 `curl`。|是|\r\n\r\n**配置后端的两种方法：**\r\n\r\n1. **服务器范围默认值** 通过 `--fetch-backend` CLI标志（适用于每个 `fetch_content` 呼叫）：\r\n\r\n   ```bash\r\n   # Default behavior — uses httpx\r\n   uvx duckduckgo-mcp-server\r\n\r\n   # Force curl for every fetch (requires the [browser] extra)\r\n   uvx --with \"duckduckgo-mcp-server[browser]\" duckduckgo-mcp-server --fetch-backend curl\r\n\r\n   # Try httpx first, fall back to curl on 403 / Cloudflare challenge\r\n   uvx --with \"duckduckgo-mcp-server[browser]\" duckduckgo-mcp-server --fetch-backend auto\r\n   ```\r\n\r\n1. **每次呼叫覆盖** 通过 `backend` 关于 `fetch_content` 工具（覆盖该单个调用的CLI默认值）。该工具暴露 `backend` 在其输入模式中，因此MCP客户端可以选择 `\"httpx\"`, `\"curl\"`，或 `\"auto\"` 以逐个提取为基础。\r\n\r\n这 `search` 工具总是使用 `httpx` --DuckDuckGo的搜索端点不需要模拟。\r\n\r\n默认值保持不变 `httpx` 因此，不需要模拟的用户不会为额外的依赖性付费。\r\n\r\n### 发展\r\n\r\n对于当地发展：\r\n\r\n```bash\r\n# Install dependencies\r\nuv sync\r\n\r\n# Run with the MCP Inspector\r\nmcp dev src/duckduckgo_mcp_server/server.py\r\n\r\n# Install locally for testing with Claude Desktop\r\nmcp install src/duckduckgo_mcp_server/server.py\r\n\r\n# Run all tests\r\nuv run python -m pytest src/duckduckgo_mcp_server/ -v\r\n\r\n# Run only unit tests\r\nuv run python -m pytest src/duckduckgo_mcp_server/test_server.py -v\r\n\r\n# Run only e2e tests\r\nuv run python -m pytest src/duckduckgo_mcp_server/test_e2e.py -v\r\n```\r\n\r\n## 可用工具\r\n\r\n### 1.搜索工具\r\n\r\n```python\r\nasync def search(query: str, max_results: int = 10, region: str = \"\") -> str\r\n```\r\n\r\n在DuckDuckGo上执行网络搜索并返回格式化结果。\r\n\r\n**参数：**\r\n\r\n- `query`：搜索查询字符串\r\n- `max_results`：要返回的最大结果数（默认值：10）\r\n- `region`：（可选）覆盖默认值的地区/语言代码。留空以使用配置的默认区域。\r\n\r\n**地区代码示例：**\r\n\r\n- `us-en`：美国（英语）\r\n- `cn-zh`：中国（中文）\r\n- `jp-ja`：日本（日语）\r\n- `de-de`：德国（德语）\r\n- `fr-fr`：法国（法语）\r\n- `wt-wt`：无特定区域\r\n\r\n**退货：**\r\n包含带有标题、URL和片段的搜索结果的格式化字符串。\r\n\r\n**示例用法：**\r\n\r\n- 使用默认设置搜索： `search(\"python tutorial\")`\r\n- 按特定地区搜索： `search(\"latest news\", region=\"jp-ja\")` 日本新闻\r\n\r\n### 2.内容获取工具\r\n\r\n```python\r\nasync def fetch_content(\r\n    url: str,\r\n    start_index: int = 0,\r\n    max_length: int = 8000,\r\n    backend: Optional[str] = None,\r\n) -> str\r\n```\r\n\r\n从网页中获取和解析内容。\r\n\r\n**参数：**\r\n\r\n- `url`：从中获取内容的网页URL\r\n- `start_index`：开始读取的字符偏移量（用于分页）\r\n- `max_length`：要返回的最大字符数\r\n- `backend`：默认获取后端的可选每次调用覆盖(`\"httpx\"`, `\"curl\"`，或 `\"auto\"`).省略时，使用通过设置的任何内容 `--fetch-backend` 在服务器启动时。\r\n\r\n**退货：**\r\n已清理并格式化网页中的文本内容。\r\n\r\n## 详细功能\r\n\r\n### 速率限制\r\n\r\n- 搜索：每分钟限制30个请求\r\n- 内容获取：每分钟限制20个请求\r\n- 自动队列管理和等待时间\r\n\r\n### 结果处理\r\n\r\n- 删除广告和无关内容\r\n- 清理DuckDuckGo重定向URL\r\n- 格式化结果以实现最佳LLM消耗\r\n- 适当截断长内容\r\n\r\n### 内容安全\r\n\r\n- **安全搜索过滤**：在服务器启动时通过配置 `DDG_SAFE_SEARCH` 环境变量\r\n\r\n  - 由管理员控制，不可由AI助手修改\r\n  - 根据所选级别过滤不合适的内容\r\n  - 使用DuckDuckGo官方 `kp` 参数\r\n\r\n- **区域定位**:\r\n\r\n  - 通过设置默认区域 `DDG_REGION` 环境变量\r\n  - AI助手可以根据搜索请求覆盖\r\n  - 提高特定地理区域的结果相关性\r\n\r\n### 错误处理\r\n\r\n- 全面的错误捕获和报告\r\n- 通过MCP上下文进行详细日志记录\r\n- 在速率限制或超时方面表现良好\r\n\r\n## 贡献\r\n\r\n欢迎问题和拉取请求！一些有待改进的领域：\r\n\r\n- 增强的内容解析选项\r\n- 用于频繁访问内容的缓存层\r\n- 额外的限速策略\r\n\r\n## 许可证\r\n\r\n该项目根据MIT许可证获得许可。\r\n\r\n## 明星历史\r\n",
 "config": {
  "ddg-search": {
   "enabled": true,
   "command": "uvx",
   "args": [
    "duckduckgo-mcp-server"
   ],
   "env": {}
  }
 }
}